{
  "name": "catan-game",
  "version": "1.0.0",
  "description": "Catan board game",
  "scripts": {
    "dev": "concurrently \"cd server && npm start\" \"cd client && npm start\"",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}